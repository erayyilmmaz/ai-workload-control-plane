apiVersion: v1
kind: Pod
metadata:
  name: tenant-quota-pod-3
  namespace: awcp-tenant-alpha
spec:
  restartPolicy: Never
  containers:
    - name: pause
      image: registry.k8s.io/pause:3.10
      resources:
        requests:
          cpu: "1"
          memory: 64Mi
        limits:
          cpu: "1"
          memory: 64Mi
