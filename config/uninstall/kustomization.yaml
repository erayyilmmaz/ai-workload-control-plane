# Deliberately excludes CRDs and Namespaces: operator removal must not remove
# AIWorkloads, their owned children, user Secrets or workload namespaces.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../rbac
  - ../manager
  - ../telemetry
  - ../policy
