# Valid AWCP-8 identity/Service fixture. It uses a placeholder image, so it is not
# a runnable demo even though its dedicated ServiceAccount is now created.
apiVersion: platform.example.io/v1alpha1
kind: AIWorkload
metadata:
  name: bootstrap-sample
  namespace: awcp-workloads
spec:
  image: ghcr.io/example/demo-agent:v1
  container:
    port: 8080
