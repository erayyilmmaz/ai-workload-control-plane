# Valid AWCP-6 Deployment fixture only. It uses a placeholder image and its
# dedicated ServiceAccount is created in AWCP-8, so it is not a runnable demo.
apiVersion: platform.example.io/v1alpha1
kind: AIWorkload
metadata:
  name: bootstrap-sample
  namespace: awcp-workloads
spec:
  image: ghcr.io/example/demo-agent:v1
  container:
    port: 8080
